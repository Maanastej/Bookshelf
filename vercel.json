{
  "buildCommand": "npm run build --prefix artifacts/mockup-sandbox && cp -r artifacts/mockup-sandbox/dist ./dist",
  "installCommand": "npm install --prefix artifacts/mockup-sandbox",
  "framework": "vite"
}
