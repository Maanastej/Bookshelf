{
  "name": "workspace",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "npm --prefix artifacts/mockup-sandbox run build",
    "typecheck": "npm --prefix artifacts/mockup-sandbox run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
